Paste this into Agent — this is everything in one shot:

"In server/routes.ts, make these changes and restart:
1. OUTBOUND MESSAGING — Fix the scheduler so it actually sends WhatsApp messages via Twilio. Add this function at the top of the file after the OpenAI client:
typescriptasync function sendWhatsAppMessage(to: string, body: string) {
  try {
    const accountSid = process.env.TWILIO_ACCOUNT_SID;
    const authToken = process.env.TWILIO_AUTH_TOKEN;
    const fromNumber = process.env.TWILIO_WHATSAPP_NUMBER;
    const response = await fetch(`https://api.twilio.com/2010-04-01/Accounts/${accountSid}/Messages.json`, {
      method: 'POST',
      headers: {
        'Authorization': 'Basic ' + Buffer.from(`${accountSid}:${authToken}`).toString('base64'),
        'Content-Type': 'application/x-www-form-urlencoded'
      },
      body: new URLSearchParams({
        From: `whatsapp:${fromNumber}`,
        To: `whatsapp:${to}`,
        Body: body
      }).toString()
    });
    return response.ok;
  } catch (e) {
    console.error('Failed to send WhatsApp message:', e);
    return false;
  }
}
Then in the daily scheduler at 07:00, replace storage.logChat with sendWhatsAppMessage(user.phoneNumber, msg). In the re-engagement check at 10:00, replace storage.logChat with sendWhatsAppMessage(user.phoneNumber, reengagementMsg). In the weekly Sunday report, replace storage.logChat with sendWhatsAppMessage(user.phoneNumber, report).
2. INFORMAL SA LANGUAGE: After the greeting guard, add detection for informal responses. If message contains 'sharp', 'ke sharp', 'sharp sharp' — treat as YES or acknowledgement and reply with the menu. If message contains 'eish' — treat as emotional and reply: 'Talk to me — what is going on? Type what you ate or how you are feeling and I will help you get back on track.' If message contains 'hayibo' — reply: 'Ha — tell me more. What happened today?' If message contains 'mara' or 'shem' — continue processing the rest of the message normally. If message is 'ke a bona', 'ke ready', 'ngiyabonga', 'siyabonga' — reply with menu.
3. EMOTIONAL INTELLIGENCE: Before the intent routing, check for emotional keywords. If message contains 'give up', 'giving up', 'cant do this', 'not working', 'no results', 'waste of money', 'useless' — reply: 'I hear you. Results are not always visible on the scale — but they are happening. Tell me: are you sleeping? Are you moving? Are you eating protein? Answer those three and we fix this together.' If message contains 'stressed', 'stress', 'anxiety', 'depressed', 'sad', 'crying' — reply: 'Stress eating is real and it is not weakness. What happened today? Talk to me — then we sort out your next meal together.' If message contains 'pregnant', 'pregnancy' — reply: 'Congratulations. Your programme needs to change immediately. Stop all intense training. Focus on walks, light movement, and high nutrition. Please consult your doctor before continuing any exercise programme with us.' and set awaitingInputType to null. If message contains 'injured', 'injury', 'hurt', 'pain in', 'sore knee', 'sore back', 'sore shoulder' — reply: 'Noted. No training on an injured body. Rest it for 48 hours. If the pain persists see a physio. You can still log food and steps — keep that going.' and set trainingMode to walk_only temporarily.
4. SUPPLEMENT GUIDANCE: If message contains 'protein shake', 'protein powder', 'creatine', 'fat burner', 'pre workout', 'supplements', 'supps' — reply: 'Supplements are optional — food comes first. If your diet is clean and consistent, protein powder can help hit your daily target. Creatine is safe and effective for strength. Fat burners are mostly marketing — avoid them. Sort your food first, then we talk supplements.'
5. BEFORE/AFTER PROMPT: In the weekly Sunday report, if the user has been active for 28 days or more, add to the end of their report: 'You have been on this programme for 4 weeks. Take a photo today — front, side, back. Same time, same lighting. This is your progress marker. You will thank yourself later.'
Make sure TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, and TWILIO_WHATSAPP_NUMBER are read from process.env. Restart the server."