Paste this into Agent:
In server/routes.ts delete the entire detectIntent function and every intent-based if statement in handleMessage. Replace the entire handleMessage function with this simpler architecture:
Every message goes directly to GPT through askCoachK with a comprehensive system prompt that includes the full client profile and a complete list of how to handle every situation. GPT decides what to do. Not keyword matching.
The new handleMessage function works like this. Get or create user. If onboarding not complete route to handleOnboarding as before. If message contains a photo process vision as before. Then build a comprehensive instruction string that tells GPT everything about this specific client and asks it to respond appropriately. Send to askCoachK. Return response.
The instruction string must include all of this:
The client's full profile — name, goal, phase, calories, protein, steps, training mode, equipment, life situation, job, injuries, age, experience, water today.
Today's context — day of week, time of day, whether it is after the 20th of the month.
Instructions for every scenario GPT might encounter — if client asks for a programme give them a full structured programme with sets reps and YouTube links. If client logs food respond with specific SA coaching on that exact food never generic advice. If client mentions being broke or month end give the R57 budget plan plus other affordable options not just eggs pilchards beans — also include: oats R15 for 500g lasts a week, peanut butter R25 lasts two weeks, brown bread R14, frozen chicken portions R40 per kg, cabbage R8, spinach R10, sweet potato R12 per kg. If client mentions diabetes restructure everything around low GI and consistent meal timing. If client mentions Ramadan restructure around Suhoor and Iftar. If client asks what they ate previously say you can only see what they logged in this conversation and list anything they mentioned. If client seems frustrated respond with empathy then one action. If client asks anything about supplements give honest SA-priced advice. If client asks about weight going up explain water retention and context before anything else.
Critical rules GPT must follow — never append warning messages about protein after already coaching on food. Never tell someone pilchards is not protein. Never say drink 2 litres of water as a default response. Never respond with Howzit a client. Always use the client's actual name. Maximum 4 sentences per response. Always end with one specific action.
Also keep these specific command handlers outside GPT — MENU returns the menu text directly. DONE logs the workout and returns a celebration. RESET resets the profile. These three only go direct. Everything else goes to GPT.
Replace max_tokens in askCoachK from 150 to 300 so GPT has room to give a full programme when asked.
Do not touch schema, database, or any other file. Restart the server.
