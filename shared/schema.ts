import { pgTable, uuid, text, integer, timestamp, index, serial, jsonb, numeric, boolean, date, uniqueIndex } from "drizzle-orm/pg-core";
import { sql } from "drizzle-orm";

// Existing schema definitions retained below.
